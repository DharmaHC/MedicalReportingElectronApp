# Apple Developer Configuration for macOS Code Signing and Notarization
#
# ⚠️ OPTIONAL: These credentials are only needed if you want code signing and notarization
# Without them, the app will still build but won't be signed (users will need to bypass Gatekeeper)
#
# To build WITHOUT signing: export CSC_IDENTITY_AUTO_DISCOVERY=false
#
# IMPORTANT: Copy this file to .env and fill in your actual credentials
# NEVER commit .env to version control!

# Your Apple Developer Account email
APPLE_ID=your-email@example.com

# App-Specific Password generated from appleid.apple.com
# Security → App-Specific Passwords
APPLE_APP_SPECIFIC_PASSWORD=xxxx-xxxx-xxxx-xxxx

# Your Apple Developer Team ID
# Find it at: https://developer.apple.com/account/ → Membership
APPLE_TEAM_ID=XXXXXXXXXX

# Optional: Specify code signing identity
# Leave commented to auto-discover the first "Developer ID Application" certificate
# CSC_NAME="Developer ID Application: Your Company Name (TEAM_ID)"

# Optional: Disable code signing for testing
# Uncomment to skip code signing during development
# CSC_IDENTITY_AUTO_DISCOVERY=false
